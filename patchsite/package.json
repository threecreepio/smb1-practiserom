{
  "name": "patchsite",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "browserslist": [
    "defaults",
    "not IE 11"
  ],
  "scripts": {
    "build": "node create-patch-info.js && parcel build index.html",
    "start": "node create-patch-info.js && parcel index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  },
  "dependencies": {
    "vcdiff-decoder": "^0.2.1"
  }
}
